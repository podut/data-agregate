apiVersion: 1

providers:
  - name: DataAgregate
    orgId: 1
    folder: DataAgregate
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
